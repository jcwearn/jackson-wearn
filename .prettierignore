# npm owns this.
package-lock.json

# Local agent state, untracked. Present only on a developer's machine, so
# formatting it would fail check runs for some people and not others.
.claude/

# Build output.
dist/

# Generated by the resume repo and refreshed by scripts/fetch-resume.ts.
# Formatting it would fight that script: Prettier rewrites it, the next build
# writes the generator's formatting back, and format:check fails in between.
src/content/resume.json
